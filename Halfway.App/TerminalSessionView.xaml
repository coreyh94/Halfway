<UserControl
    x:Class="Halfway.App.TerminalSessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="Panel"
            Background="{ThemeResource TerminalBackgroundBrush}"
            BorderBrush="{ThemeResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource SurfaceCornerRadius}"
            Padding="14,12,14,12"
            SizeChanged="Panel_SizeChanged">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: identity + status on the left, actions on the right -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText" FontSize="13" FontWeight="SemiBold" Foreground="{ThemeResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                    <Border Background="{ThemeResource ChipBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource ChipCornerRadius}" Padding="8,2,10,2" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                            <Ellipse x:Name="StatusDot" Width="7" Height="7" VerticalAlignment="Center" Fill="{ThemeResource MutedTextBrush}"/>
                            <TextBlock x:Name="StatusText" Text="DISCONNECTED" FontSize="10" FontWeight="SemiBold" CharacterSpacing="40" Foreground="{ThemeResource MutedTextBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button x:Name="StartButton" Content="Start" Style="{StaticResource PrimaryButtonStyle}" Click="StartButton_Click"/>
                    <Button x:Name="StopButton" Content="Stop" Style="{StaticResource SubtleButtonStyle}" Click="StopButton_Click" IsEnabled="False"/>
                    <Button x:Name="PowerShellButton" Content="PowerShell" Style="{StaticResource SubtleButtonStyle}" Click="PowerShellButton_Click" Visibility="Collapsed"/>
                    <Button x:Name="CodexButton" Content="Codex" Style="{StaticResource SubtleButtonStyle}" Click="CodexButton_Click" Visibility="Collapsed"/>
                    <Button x:Name="DemoAlertButton" Content="Inject demo alert" Style="{StaticResource SubtleButtonStyle}" Click="DemoAlertButton_Click" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>

            <!-- Find in terminal -->
            <Border x:Name="SearchPanel" Grid.Row="1" Visibility="Collapsed" Margin="0,0,0,10"
                    Background="{ThemeResource ControlFillBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1"
                    CornerRadius="{StaticResource ControlCornerRadius}" Padding="8,6,8,6">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SearchText" Style="{StaticResource SearchBoxStyle}" PlaceholderText="Find in terminal" TextChanged="SearchText_TextChanged" KeyDown="SearchText_KeyDown"/>
                    <TextBlock x:Name="SearchResultText" Grid.Column="1" VerticalAlignment="Center" MinWidth="64" FontSize="12" Foreground="{ThemeResource MutedTextBrush}"/>
                    <Button Grid.Column="2" Content="Previous" Style="{StaticResource SubtleButtonStyle}" FontSize="12" Padding="10,4" Click="PreviousSearchButton_Click"/>
                    <Button Grid.Column="3" Content="Next" Style="{StaticResource SubtleButtonStyle}" FontSize="12" Padding="10,4" Click="NextSearchButton_Click"/>
                    <Button Grid.Column="4" Content="Close" Style="{StaticResource SubtleButtonStyle}" FontSize="12" Padding="10,4" Click="CloseSearchButton_Click"/>
                </Grid>
            </Border>

            <!-- Terminal output -->
            <ScrollViewer x:Name="OutputScroll" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBlock x:Name="OutputText" TextWrapping="NoWrap" FontFamily="Cascadia Mono, Consolas" FontSize="13" Foreground="{ThemeResource PrimaryTextBrush}" IsTextSelectionEnabled="True"/>
            </ScrollViewer>

            <!-- Terminal input -->
            <TextBox x:Name="InputText" Grid.Row="3" Style="{StaticResource TerminalInputStyle}" Margin="0,10,0,0" PlaceholderText="Type a command and press Enter" KeyDown="InputText_KeyDown" TextChanged="InputText_TextChanged"/>
        </Grid>
    </Border>
</UserControl>
